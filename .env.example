# D&D Play-by-Post Environment Variables
# This file is a template - run ./setup.sh to generate your .env file

# =============================================================================
# DOMAIN CONFIGURATION
# =============================================================================
# Your domain name (e.g., dnd.example.com) or 'localhost' for development
DOMAIN=localhost

# Enable SSL (true for production with domain, false for localhost)
ENABLE_SSL=false

# Email for SSL certificate notifications (required if ENABLE_SSL=true)
# SSL_EMAIL=admin@example.com

# =============================================================================
# CORS CONFIGURATION
# =============================================================================
# Allowed origins for CORS (comma-separated)
# Development: http://localhost,http://localhost:3000,http://localhost:5173
# Production: https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGINS=http://localhost,http://localhost:3000,http://localhost:5173

# =============================================================================
# API & FRONTEND URLs
# =============================================================================
# Base URL for API
API_URL=http://localhost:8000

# Base URL for frontend
FRONTEND_URL=http://localhost:3000

# Public API URL (used by frontend)
PUBLIC_API_URL=http://localhost:8000
VITE_API_URL=http://localhost:8000

# Origin for SvelteKit
ORIGIN=http://localhost:3000

# =============================================================================
# APPLICATION SECURITY
# =============================================================================
# IMPORTANT: Generate secure secrets using:
# openssl rand -hex 32

# Secret key for JWT tokens (MUST be changed in production!)
SECRET_KEY=change_this_secret_key_to_something_very_secure

# JWT algorithm
ALGORITHM=HS256

# JWT token expiration (in minutes)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
POSTGRES_HOST=dnd-postgres
POSTGRES_PORT=5432
POSTGRES_DB=dnd_pbp
POSTGRES_USER=dnd
POSTGRES_PASSWORD=change_this_postgres_password

# =============================================================================
# REDIS CONFIGURATION
# =============================================================================
REDIS_HOST=dnd-redis
REDIS_PORT=6379
REDIS_PASSWORD=change_this_redis_password

# =============================================================================
# PORT CONFIGURATION
# =============================================================================
BACKEND_PORT=8000
FRONTEND_PORT=3000
HTTP_PORT=80
HTTPS_PORT=443
ADMINER_PORT=8080

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
# Debug mode (set to false in production)
DEBUG=false

# Node environment (development or production)
NODE_ENV=production
# ============================================================================
# Compendium Settings
# ============================================================================

# Auto-import SRD content on first startup if compendium is empty
AUTO_IMPORT_SRD=true

# Path to SRD JSON data files
SRD_DATA_PATH=backend/data/srd/

# Allow users to create homebrew content
ALLOW_HOMEBREW=true
